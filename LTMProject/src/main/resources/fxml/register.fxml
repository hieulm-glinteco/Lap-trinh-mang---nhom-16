<?xml version="1.0" encoding="UTF-8"?>
<?import javafx.scene.layout.StackPane?>
<?import javafx.scene.layout.VBox?>
<?import javafx.scene.control.*?>
<?import javafx.scene.image.ImageView?>
<?import javafx.scene.image.Image?>

<StackPane xmlns:fx="http://javafx.com/fxml"
           fx:controller="com.mycompany.ltmproject.controller.RegisterController"
           minWidth="1000" prefWidth="1000" maxWidth="1000"
           minHeight="650" prefHeight="650" maxHeight="650"
           stylesheets="@../css/style.css">

    <children>
        <!-- Ảnh nền -->
        <ImageView fitWidth="1000" fitHeight="700" preserveRatio="false">
            <image>
                <Image url="@../images/background.jpeg" />
            </image>
        </ImageView>

        <!-- Lớp phủ và nội dung -->
        <VBox alignment="CENTER" spacing="18"
              style="-fx-background-color: rgba(0,0,0,0.35);"
              prefWidth="1000" prefHeight="650">

            <!-- Tiêu đề -->
            <Label text="📝 ĐĂNG KÝ TÀI KHOẢN"
                   styleClass="label-title"
                   style="-fx-effect: dropshadow(gaussian, rgba(0,0,0,0.5), 10, 0.3, 0, 2);
                          -fx-font-family: 'Segoe UI', 'Arial', sans-serif;"/>

            <!-- Khung đăng ký -->
            <VBox alignment="CENTER" spacing="10"
                  maxWidth="360"
                  style="-fx-background-color: #ffffff;
                         -fx-background-radius: 20;
                         -fx-padding: 20;
                         -fx-effect: dropshadow(gaussian, rgba(0,0,0,0.4), 20, 0.4, 0, 8);">

                <!-- Họ và tên -->
                <VBox spacing="4" maxWidth="320">
                    <Label text="Họ và Tên" styleClass="label-form"/>
                    <TextField fx:id="fullNameField"
                               promptText="Nhập họ và tên đầy đủ"
                               prefHeight="25"
                               styleClass="text-field"/>
                </VBox>

                <!-- Ngày sinh -->
                <VBox spacing="4" maxWidth="320">
                    <Label text="Ngày Sinh" styleClass="label-form"/>
                    <DatePicker fx:id="dobPicker"
                                promptText="Chọn ngày sinh"
                                prefHeight="25"
                                style="-fx-background-color: white;
                                       -fx-background-radius: 8;
                                       -fx-border-color: #d1d5db;
                                       -fx-border-width: 2;
                                       -fx-border-radius: 8;"/>
                </VBox>

                <!-- Tên đăng nhập -->
                <VBox spacing="4" maxWidth="320">
                    <Label text="Tên Đăng Nhập" styleClass="label-form"/>
                    <TextField fx:id="usernameField"
                               promptText="Chọn tên đăng nhập"
                               prefHeight="25"
                               styleClass="text-field"/>
                </VBox>

                <!-- Mật khẩu -->
                <VBox spacing="4" maxWidth="320">
                    <Label text="Mật Khẩu" styleClass="label-form"/>
                    <PasswordField fx:id="passwordField"
                                   promptText="Tối thiểu 6 ký tự"
                                   prefHeight="25"
                                   styleClass="password-field"/>
                </VBox>

                <!-- Email -->
                <VBox spacing="4" maxWidth="320">
                    <Label text="Email" styleClass="label-form"/>
                    <TextField fx:id="emailField"
                               promptText="email@example.com"
                               prefHeight="25"
                               styleClass="text-field"/>
                </VBox>

                <!-- Số điện thoại -->
                <VBox spacing="4" maxWidth="320">
                    <Label text="Số Điện Thoại" styleClass="label-form"/>
                    <TextField fx:id="phoneField"
                               promptText="0122556789"
                               prefHeight="25"
                               styleClass="text-field"/>
                </VBox>

                <!-- Status Label -->
                <Label fx:id="statusLabel"
                       maxWidth="320"
                       style="-fx-text-fill: #ef4444;
                              -fx-font-size: 13px;
                              -fx-font-weight: 600;"/>

                <!-- Buttons -->
                <VBox spacing="8" alignment="CENTER" maxWidth="320">
                    <Button text="Đăng Ký Ngay"
                            prefHeight="40"
                            styleClass="btn-success"
                            onAction="#handleRegister"/>

                    <Button text="⬅  Quay Lại Đăng Nhập"
                            prefHeight="38"
                            styleClass="btn-outline"
                            onAction="#goLogin"/>
                </VBox>
            </VBox>
        </VBox>
    </children>
</StackPane>
