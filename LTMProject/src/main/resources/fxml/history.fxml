<?xml version="1.0" encoding="UTF-8"?>
<?import javafx.scene.layout.StackPane?>
<?import javafx.scene.layout.AnchorPane?>
<?import javafx.scene.layout.VBox?>
<?import javafx.scene.control.*?>
<?import javafx.scene.image.ImageView?>
<?import javafx.scene.image.Image?>
<?import javafx.geometry.Insets?>

<VBox xmlns:fx="http://javafx.com/fxml"
      fx:controller="com.mycompany.ltmproject.controller.HistoryController"
      minWidth="1000" prefWidth="1000" maxWidth="1000"
      minHeight="650" prefHeight="650" maxHeight="650"
      stylesheets="@../css/style.css">
    
    <!-- Header -->
    <fx:include fx:id="headerPane" source="header.fxml"/>
    
    <!-- Nội dung chính -->
    <StackPane VBox.vgrow="ALWAYS">
        <!-- Ảnh nền -->
        <ImageView fitWidth="1000" fitHeight="650" preserveRatio="false">
            <image>
                <Image url="@../images/background.jpeg" />
            </image>
        </ImageView>
        
        <!-- Lớp phủ -->
        <AnchorPane style="-fx-background-color: rgba(0,0,0,0.35);">
            <children>
                <VBox alignment="CENTER" spacing="25" 
                      AnchorPane.topAnchor="40" 
                      AnchorPane.leftAnchor="100" 
                      AnchorPane.rightAnchor="100" 
                      AnchorPane.bottomAnchor="40">
                    
                    <!-- Tiêu đề -->
                    <Label text="📜 LỊCH SỬ ĐẤU"
                           styleClass="label-title"
                           style="-fx-effect: dropshadow(gaussian, rgba(0,0,0,0.5), 10, 0.3, 0, 2);"/>
                    
                    <!-- Container cho bảng -->
                    <AnchorPane prefWidth="800" prefHeight="450"
                                style="-fx-background-color: #ffffff;
                                       -fx-background-radius: 20;
                                       -fx-effect: dropshadow(gaussian, rgba(0,0,0,0.4), 20, 0.4, 0, 8);">
                        <children>
                            <!-- Bảng lịch sử -->
                            <TableView fx:id="historyTable" 
                                       AnchorPane.topAnchor="25" 
                                       AnchorPane.leftAnchor="25" 
                                       AnchorPane.rightAnchor="25" 
                                       AnchorPane.bottomAnchor="90"
                                       styleClass="history-table">
                                <columns>
                                    <TableColumn fx:id="colDate" text="🕐 Thời Gian" prefWidth="200"/>
                                    <TableColumn fx:id="colOpponent" text="👤 Đối Thủ" prefWidth="200"/>
                                    <TableColumn fx:id="colStatus" text="🎯 Kết Quả" prefWidth="175"/>
                                    <TableColumn fx:id="colScore" text="⭐ Điểm Số" prefWidth="175"/>
                                </columns>
                            </TableView>
                            
                            <!-- Nút trở về -->
                            <Button fx:id="backButton" text="⬅  Trở Về Trang Chủ" 
                                    AnchorPane.bottomAnchor="25"
                                    AnchorPane.leftAnchor="275"
                                    AnchorPane.rightAnchor="275"
                                    prefHeight="45"
                                    styleClass="btn-back"
                                    onAction="#handleBack"/>
                        </children>
                    </AnchorPane>
                    
                </VBox>
            </children>
        </AnchorPane>
    </StackPane>
</VBox>